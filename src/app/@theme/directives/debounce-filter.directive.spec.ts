import { DebounceFilterDirective } from './debounce-filter.directive';

describe('DebounceFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceFilterDirective();
    expect(directive).toBeTruthy();
  });
});
